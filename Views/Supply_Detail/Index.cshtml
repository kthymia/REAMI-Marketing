@model IEnumerable<REAMI_Marketing_Sales___Inventory_System.Models.Supply_Detail>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Product
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity_Supplied)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Unit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Product.Product_Desc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity_Supplied)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Unit)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Supply_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Supply_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Supply_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
