@model IEnumerable<REAMI_Marketing_Sales___Inventory_System.Models.Order>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Customer_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Order_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Order_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Order_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
