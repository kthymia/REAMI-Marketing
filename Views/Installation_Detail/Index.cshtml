@model IEnumerable<REAMI_Marketing_Sales___Inventory_System.Models.Installation_Detail>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Employee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.Employee_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role.Role_ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Installation_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Installation_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Installation_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
